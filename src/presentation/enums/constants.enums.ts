export enum Constants {
  EMAIL_EXISTS = 'Email already exists!',
  USER_PROFILE_EXISTS = 'User profile already exists, try update',
  USER_EXISTS = 'User name already exists!',
  ROLE = 'role',
  DELETED = 'Data not founded',
}
