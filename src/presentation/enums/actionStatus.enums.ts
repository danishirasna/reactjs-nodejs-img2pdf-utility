// Define enum for action status
export enum ActionStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  CREATE = 'create',
  UPDATE = 'update',
  DELETE = 'delete',
  PENDING = 'pending',
  DONE = 'done',
  VERIFY_LICENSE = 'verify-license',
  DOWNLOAD_MAP = 'download-map',
}
